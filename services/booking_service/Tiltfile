docker_build("booking-service-image", ".", dockerfile="./Dockerfile")

yaml = helm(
  "../../deployment/charts/base",
  name="booking-service-release",
  values=["../../deployment/kubernetes/booking_service/booking.values.local.yaml"],
)
k8s_yaml(yaml)

